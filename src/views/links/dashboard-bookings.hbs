<body>
  <div class="container-fluid">
    <div class="row">
      {{> main}}

      <!-- Contenido principal   -->
      <main class=" ms-sm-auto col-lg-10">
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
          <h2 style="color: blue;">Bookings</h2>
            {{#if name}}
              <h5 style="color: gray;">Welcome <span class="badge badge-success">{{name}}</span></h5>
            {{/if}}
        </div>
        {{>bookings}} 
      </main>
    </div>
  </div>
</body>